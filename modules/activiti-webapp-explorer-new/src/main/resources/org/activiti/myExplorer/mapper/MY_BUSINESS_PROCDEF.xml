<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.activiti.myExplorer.mapper.MyBusinessProcdefMapper">
	<cache />

	<select id="select" resultMap="result">
		flying#{?}:select
	</select>

	<select id="selectAll" resultMap="result">
		flying:selectAll
	</select>

	<select id="selectOne" resultMap="result">
		flying:selectOne
	</select>

	<select id="count" resultType="int">
		flying:count
	</select>

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		flying:insert
	</insert>

	<update id="update">
		flying:update
	</update>

	<update id="updatePersistent">
		flying:updatePersistent
	</update>

	<delete id="delete">
		flying:delete
	</delete>

	<resultMap id="result" type="MyBusinessProcdef" autoMapping="true">
		<id property="id" column="ID" />
		<result property="businessId" column="BUSINESS_ID" />
		<association property="actReProcdef" javaType="ActReProcdef"
			select="org.activiti.myExplorer.mapper.ActReProcdefMapper.select"
			column="PRCODEF_ID" />
	</resultMap>
</mapper>